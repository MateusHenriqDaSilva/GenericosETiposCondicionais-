function identificar(valor: string) {
    return valor
}

function identificarString(valor: string) {
    return valor
}

function indetificar(valor: any) {
    return valor
}

const resultado = identificar('valor')
resultado.toFixed() 
// toFixed so roda em numeros fixando as casa decimais.

function Identificar<T>(valor: T) {
    return valor
}